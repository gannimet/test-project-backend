# Creating an app

```bash
mkdir mein-server && cd mein-server
npm init -y

# Laufzeit-Abhängigkeit
npm install express

# Entwicklungs-Abhängigkeiten
npm install -D typescript tsx @types/node @types/express
```
